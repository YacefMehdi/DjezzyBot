# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# FAISS index (rebuilt from data/djezzy_pages.json)
data/faiss_index/

# Scrape backups / partial scrapes / extractor drafts (never commit)
*.backup_*.json
*scrape_incomplete*
*.generated.json

# Academic records — kept LOCAL only, not pushed (Colab repo stays lean)
Mémoire Master/
Mémoire_Master/
Presentation_Defense/
docs/

# Runtime artifacts
latency_store.json
refresh_log.txt
tts_out/
*.wav

# Scratch logs
full_crawl.log
*.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# LaTeX build artifacts (thesis source is tracked; generated files are not)
*.aux
*.bbl
*.blg
*.lof
*.lot
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
*-eps-converted-to.pdf
# Office lock/temp files
~$*
